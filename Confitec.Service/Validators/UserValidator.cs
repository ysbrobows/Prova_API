using FluentValidation;
using Confitec.Domain.Entities;
using System;

namespace Confitec.Service.Validators
{
    //public UserValidator() : AbstractValidator<Usuario>
    //{
        //RuleFor(c => c.Nome)
        //    .NotEmpty().WithMessage("Por favor preencha o nome.")
        //    .NotNull().WithMessage("Por favor preencha o nome.");

        //RuleFor(c => c.Email)
        //    .NotEmpty().WithMessage("Por favor preencha o email")
        //    .NotNull().WithMessage("Por favor preencha o email");

        //RuleFor(c => c.Sobrenome)
        //    .NotEmpty().WithMessage("Por favor preencha o sobrenome")
        //    .NotNull().WithMessage("Por favor preencha o sobrenome");

        //RuleFor(c => c.Email)
        //    
        //    .NotEmpty().WithMessage("Por favor preencha o sobrenome")
        //    .NotNull().WithMessage("Please enter the password.")
        //    .EmailAddress().WithMessage("Email inválido");

    //RuleFor(c => c.Date).Must(date => date > DateTime.Now).WithMessage("Data Invalida");
    // }
}
